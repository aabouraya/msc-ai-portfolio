source "https://rubygems.org"

# Matches the GitHub Pages build environment, so a local preview renders
# exactly what the published site will. Run: bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick"
